import type { Metadata } from "next";
import { Geist, Geist_Mono } from "next/font/google";
import "./globals.css";

const geist = Geist({
  variable: "--font-geist-sans",
  subsets: ["latin"],
});

const geistMono = Geist_Mono({
  variable: "--font-geist-mono",
  subsets: ["latin"],
});

export const metadata: Metadata = {
  title: "Unive Chatbot - AI-Powered Assistant",
  description: "An intelligent chatbot built with Next.js and Vercel AI SDK, featuring OpenAI integration, custom tools, and persistent memory.",
  keywords: ["chatbot", "AI", "Next.js", "OpenAI", "Vercel AI SDK", "assistant"],
  authors: [{ name: "Unive Technical Challenge" }],
  viewport: "width=device-width, initial-scale=1",
  robots: "index, follow",
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html lang="en">
      <body
        className={`${geist.variable} ${geistMono.variable} antialiased`}
      >
        {children}
      </body>
    </html>
  );
}
